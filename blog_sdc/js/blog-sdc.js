/**
 * @file
 * Blog SDC behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.blogSdc = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
